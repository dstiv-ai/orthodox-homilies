// The app lives at /app/. Anything under the repo root (data/, assets/,
// artifact/) is reached with this prefix from any script inside app/js/.
// Later build steps (router, reading page, search) reuse this same convention.
export const ROOT = '../';
